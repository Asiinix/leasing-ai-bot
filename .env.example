# Live public reference APIs; use snapshot for an offline summit rehearsal.
COLVIR_MODE=live

# main Railway service only; used by the build to proxy /andrey.
# ANDREY_ORIGIN=http://leasing-ai-bot-andrey.railway.internal:8080

# andrey Railway service only; must be set before npm run build.
# NEXT_PUBLIC_BASE_PATH=/andrey
# PORT=8080
