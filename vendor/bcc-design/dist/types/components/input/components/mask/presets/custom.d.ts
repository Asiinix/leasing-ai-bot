import { MaskitoOptions } from '@maskito/core';
import { CustomMaskProps } from '../InputMask.type';
export declare const getCustomPreset: ({ value, prefix, postfix, placeholder, mode, }: CustomMaskProps) => MaskitoOptions;
